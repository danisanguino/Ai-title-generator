{
  "name": "title-generator",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "css": "sass --watch sass/style.scss:style.css --style compressed"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "sass": "^1.82.0"
  }
}
